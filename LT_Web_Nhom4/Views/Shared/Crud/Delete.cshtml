@model CrudDeleteViewModel

@{
    ViewData["Title"] = $"Xoá {Model.Title}";
}

@{
    var visibleFields = Model.Fields.Where(field => field.ShowInDetails).Take(6).ToList();
}

<section class="crud-page">
    <div class="container">
        <div class="card crud-form-card border-danger">
            <div class="card-body">
                <span class="crud-kicker text-danger">Xác nhận xóa</span>
                <h1>Xoá @Model.Title</h1>
                <p class="crud-form-description text-danger">Bản ghi sẽ bị xóa khỏi hệ thống sau khi xác nhận.</p>

                <dl class="crud-detail-list">
                    @foreach (var field in visibleFields)
                    {
                        <div>
                            <dt>@field.Label</dt>
                            <dd>@(field.IsBoolean ? (field.Value == "true" ? "Có" : "Không") : field.Value ?? "-")</dd>
                        </div>
                    }
                </dl>

                <form asp-area="@Model.AreaName" asp-controller="@Model.ControllerName" asp-action="Delete" asp-route-id="@Model.Key" method="post">
                    <div class="crud-form-actions">
                        <a asp-area="@Model.AreaName" asp-controller="@Model.ControllerName" asp-action="Index" class="btn btn-light">Huỷ</a>
                        <button type="submit" class="btn btn-danger">Xoá</button>
                    </div>
                </form>
            </div>
        </div>
    </div>
</section>
